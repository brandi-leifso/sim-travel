{
  "name": "golden-egg",
  "version": "1.0.0",
  "private": true,
  "description": "VClub Golden Egg Rebuy — standalone live in-room promo (public Display + floor Admin). No live-data access.",
  "scripts": {
    "install:all": "cd web && npm install && cd ../server && npm install",
    "build": "cd web && npm install && npm run build",
    "start": "cd server && npm install && node index.js",
    "dev:web": "cd web && npm run dev",
    "dev:server": "cd server && node index.js"
  }
}
